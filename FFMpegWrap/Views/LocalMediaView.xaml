<UserControl x:Class="FFMpegWrap.Views.LocalMediaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="8" Background="{DynamicResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="220" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="12"
                Margin="0,0,0,12"
                Background="{DynamicResource AppSurfaceBrush}"
                BorderBrush="{DynamicResource AppBorderBrush}"
                BorderThickness="1"
                CornerRadius="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2.2*" />
                    <ColumnDefinition Width="170" />
                    <ColumnDefinition Width="170" />
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="140" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,6" Text="Selected file" FontWeight="SemiBold" />
                <TextBlock Grid.Row="0" Grid.Column="1" Margin="0,0,12,6" Text="Tool" FontWeight="SemiBold" />
                <TextBlock Grid.Row="0" Grid.Column="2" Margin="0,0,12,6" Text="Format" FontWeight="SemiBold" />
                <TextBlock Grid.Row="0" Grid.Column="3" Margin="0,0,12,6" Text="Audio bitrate" FontWeight="SemiBold" />
                <TextBlock Grid.Row="0" Grid.Column="4" Margin="0,0,12,6" Text="Video bitrate" FontWeight="SemiBold" />
                <TextBlock Grid.Row="0" Grid.Column="5" Margin="0,0,0,6" Text="Actions" FontWeight="SemiBold" />

                <TextBox Grid.Row="1" Grid.Column="0" Margin="0,0,12,8" Text="{Binding SelectedFilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,0,12,8" ItemsSource="{Binding Tools}" SelectedItem="{Binding SelectedTool}" />
                <ComboBox Grid.Row="1" Grid.Column="2" Margin="0,0,12,8" ItemsSource="{Binding OutputFormats}" SelectedItem="{Binding SelectedOutputFormat}" />
                <TextBox Grid.Row="1" Grid.Column="3" Margin="0,0,12,8" Text="{Binding AudioBitrate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Grid.Row="1" Grid.Column="4" Margin="0,0,12,8" Text="{Binding VideoBitrate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <StackPanel Grid.Row="1" Grid.Column="5" Orientation="Horizontal">
                    <Button Margin="0,0,8,8" Padding="18,8" Command="{Binding BrowseFilesCommand}" Content="Browse..." />
                    <Button Margin="0,0,0,8" Padding="18,8" Command="{Binding ExecuteToolCommand}" Content="Run" />
                </StackPanel>

                <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,12,6" Text="Output folder (optional)" FontWeight="SemiBold" />
                <TextBox Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="4" Margin="0,0,12,0" Text="{Binding OutputDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Row="2" Grid.Column="5" Padding="14,6" Command="{Binding BrowseOutputFolderCommand}" Content="Choose folder" />
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="Batch Files" Margin="0,0,12,0">
                <ListBox Margin="8" ItemsSource="{Binding SelectedFiles}" SelectedItem="{Binding SelectedFilePath, Mode=TwoWay}" />
            </GroupBox>

            <Grid Grid.Column="1" Margin="0,0,12,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="3*" />
                    <RowDefinition Height="2*" />
                </Grid.RowDefinitions>
                <GroupBox Grid.Row="0" Header="Preview" Margin="0,0,0,12">
                    <DockPanel Margin="8">
                        <StackPanel DockPanel.Dock="Bottom"
                                    Margin="0,8,0,0"
                                    Orientation="Horizontal"
                                    HorizontalAlignment="Center">
                            <Button Margin="0,0,8,0"
                                    Padding="12,6"
                                    Click="OnRewindPreviewClick"
                                    Content="-5s" />
                            <Button Margin="0,0,8,0"
                                    Padding="12,6"
                                    Click="OnPlayPausePreviewClick"
                                    Content="Play / Pause" />
                            <Button Margin="0,0,8,0"
                                    Padding="12,6"
                                    Click="OnStopPreviewClick"
                                    Content="Stop" />
                            <Button Padding="12,6"
                                    Click="OnForwardPreviewClick"
                                    Content="+5s" />
                        </StackPanel>

                        <Border Background="{DynamicResource AppPreviewBrush}" CornerRadius="4">
                            <MediaElement x:Name="PreviewPlayer"
                                          LoadedBehavior="Manual"
                                          UnloadedBehavior="Manual"
                                          Stretch="Uniform" />
                        </Border>
                    </DockPanel>
                </GroupBox>

                <GroupBox Grid.Row="1" Header="Metadata">
                    <ListView Margin="8" ItemsSource="{Binding Metadata}">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Property" Width="140" DisplayMemberBinding="{Binding Name}" />
                                <GridViewColumn Header="Value" Width="320" DisplayMemberBinding="{Binding Value}" />
                            </GridView>
                        </ListView.View>
                    </ListView>
                </GroupBox>
            </Grid>

            <GroupBox Grid.Column="2" Header="Command Options">
                <DockPanel Margin="8">
                    <Button DockPanel.Dock="Top" Margin="0,0,0,8" HorizontalAlignment="Left" Padding="14,6" Command="{Binding RefreshOptionsCommand}" Content="Load live options" />
                    <DataGrid ItemsSource="{Binding ToolOptions}"
                              AutoGenerateColumns="False"
                              HeadersVisibility="Column"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              IsReadOnly="False">
                        <DataGrid.Columns>
                            <DataGridCheckBoxColumn Header="Use" Binding="{Binding IsEnabled, Mode=TwoWay}" Width="60" />
                            <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="140" IsReadOnly="True" />
                            <DataGridTextColumn Header="Flag" Binding="{Binding Flag}" Width="140" IsReadOnly="True" />
                            <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="260" IsReadOnly="True" />
                            <DataGridTextColumn Header="Value" Binding="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="160" />
                            <DataGridTextColumn Header="Example" Binding="{Binding ExampleUsage}" Width="320" IsReadOnly="True" />
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </GroupBox>
        </Grid>

        <GroupBox Grid.Row="2" Margin="0,12,0,12" Header="Generated Command Preview">
            <StackPanel Margin="8">
                <TextBlock Foreground="{DynamicResource AppMutedForegroundBrush}" Text="{Binding StatusMessage}" TextWrapping="Wrap" />
                <TextBox Margin="0,8,0,0"
                         Text="{Binding GeneratedCommandLine, Mode=OneWay}"
                         IsReadOnly="True"
                         BorderThickness="0"
                         Background="{DynamicResource AppSurfaceBrush}"
                         TextWrapping="Wrap" />
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="3" Header="Execution Log">
            <TextBox Margin="8"
                     Text="{Binding ExecutionLog, Mode=OneWay}"
                     IsReadOnly="True"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     TextWrapping="Wrap" />
        </GroupBox>
    </Grid>
</UserControl>
