<UserControl x:Class="FFMpegWrap.Views.OnlineMediaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="8" Background="{DynamicResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="yt-dlp Request" Margin="0,0,0,12">
            <Grid Margin="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="280" />
                    <ColumnDefinition Width="260" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,0,6" Text="Video or playlist URL" FontWeight="SemiBold" />
                <TextBlock Grid.Row="0" Grid.Column="1" Margin="12,0,0,6" Text="Output name" FontWeight="SemiBold" />
                <TextBlock Grid.Row="0" Grid.Column="2" Margin="12,0,0,6" Text="Output folder" FontWeight="SemiBold" />
                <TextBox Grid.Row="1" Grid.Column="0" Margin="0,0,12,0" Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,12,0" Text="{Binding OutputName, UpdateSourceTrigger=PropertyChanged}" />
                <TextBox Grid.Row="1" Grid.Column="2" Margin="0,0,12,0" Text="{Binding OutputDirectory, UpdateSourceTrigger=PropertyChanged}" />
                <Button Grid.Row="1" Grid.Column="3" Margin="0,0,12,0" Padding="14,8" Command="{Binding BrowseOutputFolderCommand}" Content="Browse..." />
                <Button Grid.Row="1" Grid.Column="4" Margin="0,0,12,0" Padding="14,8" Command="{Binding LoadFormatsCommand}" Content="Load Formats" />
                <Button Grid.Row="1" Grid.Column="5" Padding="14,8" Command="{Binding DownloadCommand}" Content="Download" />
            </Grid>
        </GroupBox>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <GroupBox Grid.Column="0" Header="Available Formats" Margin="0,0,12,0">
                <ListBox Margin="8" ItemsSource="{Binding Formats}" SelectedItem="{Binding SelectedFormat}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,8">
                                <TextBlock FontWeight="SemiBold" Text="{Binding DisplayName}" />
                                <TextBlock Opacity="0.8" Text="{Binding Notes}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </GroupBox>

            <GroupBox Grid.Column="1" Header="Download Log">
                <TextBox Margin="8"
                         Text="{Binding DownloadLog}"
                         IsReadOnly="True"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         TextWrapping="Wrap" />
            </GroupBox>
        </Grid>

        <StackPanel Grid.Row="2" Margin="0,12,0,0">
            <TextBlock Margin="0,0,0,6" Text="{Binding StatusMessage}" FontWeight="SemiBold" />
            <ProgressBar Height="18" Minimum="0" Maximum="100" Value="{Binding DownloadProgress}" />
        </StackPanel>
    </Grid>
</UserControl>

