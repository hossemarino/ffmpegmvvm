<UserControl x:Class="FFMpegWrap.Views.ToolPathsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="8" Background="{DynamicResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="ffmpeg.exe" FontWeight="SemiBold" />
        <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,12,12" Text="{Binding FfmpegPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <Button Grid.Row="0" Grid.Column="2" Padding="14,6" Command="{Binding BrowseFfmpegCommand}" Content="Browse..." />

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="ffplay.exe" FontWeight="SemiBold" />
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,12,12" Text="{Binding FfplayPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <Button Grid.Row="1" Grid.Column="2" Padding="14,6" Command="{Binding BrowseFfplayCommand}" Content="Browse..." />

        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="ffprobe.exe" FontWeight="SemiBold" />
        <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,12,12" Text="{Binding FfprobePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <Button Grid.Row="2" Grid.Column="2" Padding="14,6" Command="{Binding BrowseFfprobeCommand}" Content="Browse..." />

        <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="yt-dlp.exe" FontWeight="SemiBold" />
        <TextBox Grid.Row="3" Grid.Column="1" Margin="0,0,12,12" Text="{Binding YtDlpPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <Button Grid.Row="3" Grid.Column="2" Padding="14,6" Command="{Binding BrowseYtDlpCommand}" Content="Browse..." />

        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,8,0,12" Orientation="Horizontal">
            <Button Margin="0,0,8,0" Padding="18,8" Command="{Binding SaveCommand}" Content="Save" />
            <Button Padding="18,8" Command="{Binding ReloadCommand}" Content="Reload" />
        </StackPanel>

        <GroupBox Grid.Row="5" Grid.Column="0" Grid.ColumnSpan="3" Header="Status">
            <TextBlock Margin="8" Text="{Binding StatusMessage}" TextWrapping="Wrap" />
        </GroupBox>
    </Grid>
</UserControl>
